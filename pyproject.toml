[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "coae"
version = "0.1.0"
description = "Cosmos Oculto Automation Engine"
requires-python = ">=3.12"

[project.scripts]
coae = "coae.cli:main"

[tool.setuptools.packages.find]
include = ["coae*"]